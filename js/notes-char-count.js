// $('.cw_block_collection .notes_field.notes_edited .notes_show .notes_text').first().text().length

// $('.cw_block_wantlist .notes_field.notes_edited .notes_show .notes_text').first().text().length

// when editing, this is the char count
// $('.cw_block_collection .notes_field .notes_edit .notes_textarea').val().length

// length of focus input
// setInterval(function(){console.log($(':focus').val().length)}, 1000)

// .notes_show trigger?
